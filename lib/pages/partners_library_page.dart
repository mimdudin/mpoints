import 'package:flutter/material.dart';

class PartnersLibraryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Partners Library"),
      ),
      body: Center(
        child: Text("Partners Library page here."),
      ),
    );
  }
}
