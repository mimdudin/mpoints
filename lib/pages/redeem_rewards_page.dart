import 'package:flutter/material.dart';

class RedeemRewardsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Redeem Rewards"),
      ),
      body: Center(
        child: Text("Redeem Rewards page here."),
      ),
    );
  }
}
