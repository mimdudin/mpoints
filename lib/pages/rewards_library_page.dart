import 'package:flutter/material.dart';

class RewardsLibraryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Rewards Library"),
      ),
      body: Center(
        child: Text("Rewards Library page here."),
      ),
    );
  }
}
